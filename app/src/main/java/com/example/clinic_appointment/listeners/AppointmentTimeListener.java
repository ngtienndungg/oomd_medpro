package com.example.clinic_appointment.listeners;

import com.example.clinic_appointment.models.AppointmentTime.AppointmentTime;

public interface AppointmentTimeListener {
    void onClick(AppointmentTime appointmentTime);
}
